import os
PROJECT_ROOT = os.path.abspath(os.path.dirname(__file__))
LOG_PATH = os.path.abspath(os.path.join(PROJECT_ROOT, 'log'))


TOKEN = 'my token'
CHANNEL_ID = 'channel id here' # The ID of the channel the bot should talk in by default.
OWNER_ID = 'owner id here' # The ID of the person the bot should listen to for sensitive commands.

DEBUG = False

ENABLED_EXTENSIONS = [
    'control',
]